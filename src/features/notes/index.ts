/**
 * API pública da feature de notas.
 *
 * O que estiver fora desta lista é detalhe interno: mover ou renomear um
 * arquivo de `components/` não deve quebrar ninguém de fora.
 */

export { EditorPane } from "./components/EditorPane";
export { NoteList } from "./components/NoteList";
export { PropertiesPanel } from "./components/PropertiesPanel";
/** O fluxo de processamento reaproveita o seletor de conexões. */
export { ConnectionsDialog } from "./components/ConnectionsDialog";
export { KindBadge } from "./components/KindBadge";
